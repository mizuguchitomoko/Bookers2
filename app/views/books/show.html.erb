<h1>Book detail</h1>
<table class="table">
	<thead>
		<tr>
			<th>image</th>
			<th>name</th>
			<th></th>

		</tr>
	</thead>

	<tbody>

		<tr>
      <td> profiel</td>
      <td><%= link_to @user.name, user_path(@book.user.id) %></td>
      <td><%= link_to @book.title,book_path(@book)%></td>
      <td><%= @book.body %></td>
       <% if @user.id == current_user.id %>
      <td><%= link_to "edit", edit_book_path(@book)  %></td>
		  <td><%= link_to "destroy", book_path(@book), method: :delete %></td>
		 </tr>
     <% end %>

  </tbody>
</table>

