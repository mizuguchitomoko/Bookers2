

<% if @book.errors.any? %>
  <div class="alert alert-warning">
    <div class="erro"><ul>error!
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %></div>
    </ul>
  </div>
<% end %>